# 2026-03-20 工作日志

## 修复 #1：修复上一轮 Token 验证发现的 7 个问题

### 已修复内容
1. **SEARCH_COMPONENT_SPEC.md §7** — 设计令牌表旧名替换为标准 CSS Token 名（如 `--color-bg-item`、`--color-fill-standard-brand` 等）
2. **DIALOG_COMPONENT_SPEC.md §3.2** — 颜色规范表中文旧名替换为标准 CSS Token 名（如 `--color-overlay-dialog`、`--color-bg-item` 等）
3. **LIST_COMPONENT_SPEC.md** — 修复章节编号混乱，原来的重复 §5 改为 §6（列表组合规则），§7 CSS 实现参考内的 §6.3 改为 §7.3，重复 §7 交互行为改为 §8
4. **LIST_COMPONENT_SPEC.md §5** — `--文本色-text_link` 改为 `--color-text-link`
5. **search.json** — `layout.outerContainer.background` 从硬编码 `"white"` 改为 `{"token":"bg_item","value":"#FFFFFF"}`
6. **SEARCH_COMPONENT_SPEC.md §3.1/3.2** — 硬编码色值改为 Token 引用形式 `var(--color-bg-item)`、`var(--color-fill-standard-brand)`
7. **DIALOG_COMPONENT_SPEC.md §3.2** — 辅助信息文案 Token 从错误的 `text_secondary` 修正为 `--color-text-tertiary`（0.56 透明度对应 tertiary）

## 验证测试 #2：Token 替换正确性验证（token-test.html v2）

### 方法
完全不读取现有 HTML，仅基于 MD 文档 + JSON + CSS Token 从零生成 `token-test.html`，随机抽取 3 个新组件渲染并验证。

### 抽取的组件
1. **Button 按钮**：12 变体（4尺寸 × 3类型）+ 交互状态展示
2. **ActionSheet 操作面板**：22 变体（全部）
3. **NavBar 导航栏**：15 代表性变体（覆盖所有 L/C/R 类型）

### 发现的静态文档不一致
详见下方问题清单

## 修复 #3：修复第二轮 Token 验证发现的 6 个问题

### 已修复内容
1. **ACTION_SHEET_COMPONENT_SPEC.md §3.2** — 颜色规范表 Token 列旧中文名（`overlay_dark`、`背景色-Secondary`、`文本色-text_secondary` 等）全部替换为标准 CSS Token 名（`--color-overlay-dark`、`--color-bg-secondary`、`--color-text-secondary` 等）
2. **ACTION_SHEET_COMPONENT_SPEC.md §8 CSS 代码块** — 5 处旧 Token fallback（`var(--背景色-Secondary, ...)`、`var(--分割色-Separators, ...)`、`var(--文本色-text_primary, ...)`、`var(--文本色-text_secondary, ...)`、`var(--反馈色-feedback_error, ...)`）全部改为直接使用标准 `var(--color-*)` 单一格式
3. **ACTION_SHEET_COMPONENT_SPEC.md §8** — 章节编号跳跃修复：§8.5→§8.4、§8.6→§8.5、§8.7→§8.6，编号连续
4. **NAVBAR_COMPONENT_SPEC.md §6 CSS 代码块** — 8 处旧 Token fallback（`var(--bg-item, ...)`、`var(--text-primary, ...)`、`var(--text-tertiary, ...)`、`var(--brand-blue, ...)`）全部改为直接使用标准 `var(--color-*)` 单一格式
5. **action-sheet.json** — `structure.mainBlock.background` 从硬编码 `"white"` 改为 `{"token":"bg_item","value":"#FFFFFF"}`
6. **action-sheet.json** — `structure.cancelBlock.background` 从硬编码 `"white"` 改为 `{"token":"bg_item","value":"#FFFFFF"}`

## 更新 #4：README.md 新增 Icons_Library 图标库规则

### 变更内容
- 资源包结构树中新增 `icons/Icons_Library/` 目录及 6 个子文件夹说明
- 强制检查清单中新增 **步骤 5：图标替换**，包含：
  - `empty_icon.svg` 为占位图标，实际设计任务中必须替换
  - 默认使用 `QUI_24_icons/`（437 枚，覆盖最全）
  - 同一任务只允许使用一种风格（一个文件夹）
  - 6 个图标库清单表格（QUI/Core/Feather/Flex/flex_line/Material3）
- 原步骤 5（输出审查）顺延为步骤 6
- 版本日志新增 2026-03-20 条目
