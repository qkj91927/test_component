# 2026-03-19 工作日志

## Bug 修复 #1：搜索框填充色统一

### 问题
搜索框输入框的背景色在各资源文件中不一致：
- HTML 实现已改为灰色 `rgba(13, 16, 49, 0.04)` ✅
- 但 MD/JSON/CSS Token 仍为旧值 `rgba(0, 81, 254, 0.04)` ❌

### 修改文件
1. `md/SEARCH_COMPONENT_SPEC.md` — 第29行和第108行的背景色值
2. `json/components/search.json` — inputBox.background.value
3. `css/tokens.css` — `--color-fill-standard-brand` 值和注释
4. `json/index.json` — `fill_standard_brand` 值

### 统一结果
所有位置均为 `rgba(13, 16, 49, 0.04)`（灰色调），Token 名称从"品牌标准填充"改为"标准填充"。

---

## Bug 修复 #2：Checkbox SVG 尺寸不一致

### 问题
- `icons/Checkbox.svg`（未选中）：SVG 固有尺寸 20×20 ✅
- `icons/Checkbox_filled.svg`（选中）：SVG 固有尺寸 **24×24** ❌
- 切换勾选状态时会因 SVG 固有尺寸差异导致显示跳变

### 修改文件
1. `icons/Checkbox_filled.svg` — 从 24×24 等比缩放为 20×20（viewBox、所有坐标按 20/24 比例重新计算）
2. `md/DIALOG_COMPONENT_SPEC.md` — 勾选项说明增加两个 SVG 文件名和"固有尺寸均为 20×20"的说明
3. `md/ACTION_COMPONENT_SPEC.md` — 资源映射更新为 Checkbox.svg/Checkbox_filled.svg，增加尺寸一致性注意事项
4. `json/components/dialog.json` — checkIcon 增加 unchecked/checked 文件名
5. `json/components/action-combo.json` — checkboxProperties 增加 svgIntrinsicSize/unchecked/checked 字段

### 设计规则确认
- Dialog 勾选项：20×20px（内联 style 控制）
- ActionCombo A7 勾选项：20×20px（CSS class `.action-checkbox-item img` 控制）
- ActionCombo B1 勾选框：16×16px（CSS class `.action-row .action-checkbox` 控制）
- 两个 SVG 文件固有尺寸统一为 20×20，不同场景通过 CSS 缩放

---

## Bug 修复 #3：test-components.html 中 L6·C2·R3 非法列表组合

### 问题
`test-components.html` 中存在 `L6·C2·R3`（缩略图·双行·辅助+图片+箭头）组合，但根据约束规则 `R3 → C = C1`（R3 仅限单行），而 L6 是 multi 类型仅匹配 C2/C3，因此 L6+R3 永远不可能合法。

### 修改
将 `L6·C2·R3` 替换为合法的 `L6·C2·R1`（缩略图·双行·辅助+箭头），右侧区域从"辅助+图片+箭头"改为"辅助+箭头"。

### 约束确认
- `R3 → C = C1`（辅助+图片+箭头仅限单行） — component-matrix.html 第2462行和 component-builder.html 第2303行都已正确实现此约束
- L6 类型为 multi → 仅匹配 C2/C3 → 与 R3 的 C1 约束冲突 → L6+R3 永远非法

---

## 功能补充 #4：可交互组件的状态切换规范

### 问题
开关、勾选、页签、轮播指示器等可交互组件只有静态展示，缺少点击切换交互和后续状态定义。

### 新增交互（4 类组件）

| 组件 | 交互方式 | 状态切换 | CSS 实现 |
|------|---------|---------|---------|
| 开关 Switch | 点击切换开/关 | `classList.toggle('off')` | `.form-switch.off`/`.list-row .switch.off`：背景 `rgba(120,120,128,0.16)` + 滑块居左 |
| 勾选 Checkbox | 点击切换选中/未选中 | 替换 `img.src`（`Checkbox.svg` ↔ `Checkbox_filled.svg`） | 图标文件本身表达状态 |
| 页签 Tab | 点击切换选中态 | `.selected` class 互斥切换 | `.df-tab-item.selected`：加粗 500 + 指示器可见 |
| 轮播指示器 | 点击切换激活态 | `.active`/`.inactive` class 切换 | `.dot.active`：`#0099FF`；`.dot.inactive`：`rgba(13,16,49,0.04)` |

### 修改文件清单

**HTML（交互事件 + CSS 样式）**：
1. `component-builder.html` — 添加全局事件委托 + switch/dot 的 off/cursor/transition CSS
2. `component-matrix.html` — 同上
3. `test-components.html` — 同上

**MD 规范文档**：
4. `md/IMAGE_BLOCK_COMPONENT_SPEC.md` — 第七章交互行为重写，补充指示器点击切换规范 + CSS/JS 参考代码
5. `md/GROUPED_LIST_COMPONENT_SPEC.md` — 7.2 节补充 Switch 开/关态 CSS（含 `.off` class）+ 7.2.1 新增 Checkbox 交互规范
6. `md/DATA_FILTER_COMPONENT_SPEC.md` — 新增第 10 章交互行为，补充页签/分段选择/标签/下拉筛选/面包屑的交互规范

**JSON 数据**：
7. `json/components/image-block.json` — interaction 对象从简单字符串改为结构化的轮播指示器交互定义
8. `json/components/grouped-list.json` — R4/R5 交互补充 cssToggle、checkedIcon 等字段；states 补充 cssClass 字段
9. `json/components/data-filter.json` — interaction 对象从简单字符串改为结构化的页签/分段选择/标签交互定义

---

## 功能补充 #5：创建 README.md 全局指南（解决约束泄漏问题）

### 问题根因分析
尽管 HTML 代码中有约束逻辑（如 `if (R.id === 'R3' && C.id !== 'C1') return`），test-components.html 仍出现非法组合 L6·C2·R3。根因：
1. 约束分散在各 MD 文件中，无统一"强制检查清单"
2. 缺少根目录入口文件，AI 模型无法一次性获取全局规则
3. 约束仅存在于动态渲染 JS 中，手工编写 HTML 无自动过滤
4. 缺少背景色约束文档

### 创建的文件
`README.md` — 包含：
- 资源包结构说明和 AI 模型文件读取优先级
- 18 个母组件列表（导航→数据→操作→模态排序）
- 全局硬性约束（7大类）：通栏式列表组合约束、卡片式列表约束、导航栏约束、页面背景色约束、组合通用规则、可交互组件状态约束
- 设计 Token 快速参考
- AI 模型使用规范（生成前检查清单 + 验证伪代码 + 错误示例）

---

## 功能补充 #6：组件与页面背景色约束

### 新增约束规则
- 卡片式列表（Grouped List）→ 页面背景色必须为 `#F0F0F2`（bg_bottom_standard）
- 卡片（Card）→ 页面背景色必须为 `#F0F0F2`（bg_bottom_standard）
- 其他组件 → 页面背景色默认 `#FFFFFF`（bg_bottom_light）
- 混合页面以灰色背景为准

### 三种页面底色 Token
| Token | 色值 | 使用条件 |
|-------|------|---------|
| bg_bottom_light | #FFFFFF | 默认（白色） |
| bg_bottom_standard | #F0F0F2 | 含卡片式列表/卡片（浅灰） |
| bg_bottom_brand | #EFF4FF | 品牌定制（浅蓝） |

### 修改文件清单
1. `README.md` — 新建，全局指南（§3.4 页面背景色约束 + 组件-背景色对照表 + 检查清单）
2. `json/index.json` — tokens.color 新增 bg_bottom_light/standard/brand；composition.rules 新增2条背景色规则；新增 pageBackgroundConstraint 结构化定义
3. `css/tokens.css` — 新增 --color-bg-bottom-light/standard/brand 变量
4. `md/GROUPED_LIST_COMPONENT_SPEC.md` — §4 背景色说明补充 Token 名和"⚠️ 必须"约束；§4.1 CSS 注释强调
5. `md/LIST_COMPONENT_SPEC.md` — §6.3 新增"页面背景色"小节
6. `md/CARD_COMPONENT_SPEC.md` — §3 基础容器后新增"⚠️ 页面背景色约束"说明
7. `json/components/grouped-list.json` — 新增 pageBackgroundConstraint 字段
8. `json/components/card.json` — 新增 pageBackgroundConstraint 字段
9. `component-builder.html` — 新增 autoAdjustFrameBackground() 函数，在拖入/移出组件时自动检测并切换 frame 背景色

---

## 功能补充 #7：README.md 重构（约束规则下沉到各组件 MD）

### 原则
- 各组件的硬性约束规则属于各自的 `md/<COMPONENT>_SPEC.md`，不在 README 中重复
- README 只作为入口指南：资源包结构 + 母组件索引 + 精简检查清单 + Token 速查

### 修改内容
1. `README.md` — 大幅精简，从 283 行缩减至约 100 行：
   - 删除 §3.1~3.3 的组件排列组合约束细节（通栏列表/卡片列表/导航栏）
   - 删除 §3.5 组件组合通用规则的详细表格
   - 删除 §3.6 可交互组件状态约束的详细表格
   - 删除 §5.2/5.3 验证伪代码
   - 删除 §6 错误示例
   - 保留 §3.4 页面背景色约束（精简为检查清单中的一步）
   - 新增"生成前强制检查清单"（4个步骤，指向各组件 MD）
2. `md/ACTION_SHEET_COMPONENT_SPEC.md` — 新增 §6.4 模态组件嵌套约束（禁止与 Dialog/HalfScreenOverlay 嵌套）
3. `md/DIALOG_COMPONENT_SPEC.md` — §6.1 补充禁止与其他模态组件嵌套
4. `md/HALF_SCREEN_OVERLAY_COMPONENT_SPEC.md` — §7.5 内容区域说明补充禁止嵌套模态组件

---

## 功能补充 #8：设计 Token 审查 + 硬编码替换

### tokens.css 补充的 Token（7个新增）
- `--color-feedback-success: #34C759` — 成功/开关开启绿
- `--color-border-stroke: rgba(60,60,67,0.25)` — 描边色
- `--color-border-disabled: rgba(60,60,67,0.12)` — 禁用态描边
- `--color-overlay-toast: rgba(0,0,0,0.80)` — Toast 背景
- `--color-fill-pressed-brand: rgba(204,204,204,0.30)` — 按钮按压态
- `--color-handle: rgba(60,60,67,0.30)` — 把手/指示条
- `--color-switch-off: #E5E5EA` — 开关关闭态
- `--color-brand-light: rgba(0,153,255,0.10)` — 品牌色浅底

### HTML 文件硬编码替换
- `component-matrix.html`：290+ 处改用 `var(--token)`，`:root` 扩展至 30+ 变量
- `component-builder.html`：414+ 处改用 `var(--token)`，`:root` 扩展至 36+ 变量
- 保留不替换的：JS 中颜色比较逻辑的字面值、搭建器自身 UI 框架样式（调试边框、侧边栏等）
- 备份文件 `.bak` 已删除

### MD 文件代码块硬编码替换
- 全部 18 个组件 SPEC 文档的 CSS 代码块中硬编码色值替换为 `var(--token)` 格式
- 总计替换 **180+ 行**
- 修正旧中文变量名（如 `var(--背景色-Tertiary, white)`、`var(--文本-primary, black)`）为标准英文 Token（`var(--color-bg-item)` 等）
- 修正简写变量名（如 `var(--bg-item)`、`var(--text-link)`、`var(--feedback-blue)`）为完整 Token
- 特殊色值保留不替换：disabled 态半透明色（`rgba(0,153,255,0.50)` 等）、设计稿标注色、无精确 Token 映射的微妙透明度值
- 同时清理了所有 `.bak` 和 `.bak2` 备份文件

---

## 验证测试 #9：Token 替换正确性验证（token-test.html）

### 方法
完全不读取现有 HTML（component-matrix.html / component-builder.html），仅基于 MD 文档 + JSON + CSS Token 从零生成 `token-test.html`，随机抽取 3 个组件渲染并验证。

### 抽取的组件
1. **Search 搜索框**：6 变体（A1-A3 一级, B1-B3 二级）
2. **Dialog 对话框**：15 变体（标题×正文×操作）
3. **List 通栏式列表**：抽样 10 变体

### 运行时 Token 验证
- 自动检测所有组件的 computed style，验证 Token CSS 变量是否正确解析
- 全部 Token 渲染正确，无运行时异常

### 发现的静态文档不一致（7 项）
1. SEARCH_COMPONENT_SPEC.md §7 设计令牌表仍用旧 Token 名（代码块已修正，说明表未更新）
2. DIALOG_COMPONENT_SPEC.md §3.2 颜色规范表仍用旧中文名
3. LIST_COMPONENT_SPEC.md 章节编号混乱（两个§5，两个§7）
4. LIST_COMPONENT_SPEC.md §5 文字规范仍用旧 Token 名
5. search.json layout.outerContainer.background 仍用硬编码 "white"
6. SEARCH_COMPONENT_SPEC.md §3.1/3.2 描述表格中用硬编码色值
7. DIALOG_COMPONENT_SPEC.md §3.2 辅助信息文案 Token 名与色值不匹配（text_secondary vs 0.56 透明度）
