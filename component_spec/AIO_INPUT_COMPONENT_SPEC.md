# AIO 输入框 AIOInput 组件规范

> 组件类型：AIO 输入框（AIO Input）
> 组件 ID：`aio_input`
> 大类：操作
> 变体数量：3 种子组件（通过分段选择器切换）

---

## 1. 组件概述

AIO 输入框是用于 AI 对话场景的底部固定输入组件，集成了文字输入、语音输入功能。组件采用胶囊形设计，通过不同状态展示默认态、生成中态和输入态。

### 适用场景
- AI 助手对话界面
- 智能问答输入
- 语音转文字交互
- 聊天消息发送

---

## 2. 交互状态切换

AIO 输入框通过 **3 段式分段选择器** 在以下三种交互状态之间切换：

| 状态 | 编号 | 分段选择标签 | 说明 |
|------|------|-------------|------|
| 默认态 | I1 | 默认态 | 用户未输入时的初始状态，显示占位文字"发消息" |
| 生成中态 | I2 | 生成中态 | AI 正在生成回复时的状态，显示音波动画图标 |
| 输入态 | I3 | 输入态 | 用户正在输入文字时的状态，显示实际输入内容 |

### 分段选择器交互规则
- 分段选择器位于变体矩阵控件栏，用于切换展示不同交互状态
- 三个状态选项并列排列，当前激活项高亮显示
- 点击不同选项可切换对应的组件状态预览
- 分段选择器样式复用系统通用的 `mc-segmented-bar` 样式

---

## 2. 组件结构

### 整体容器
| 属性 | 值 |
|------|------|
| 宽度 | 428px（满屏宽） |
| 高度 | 72px |
| 背景 | 透明 + backdrop-filter: blur(25px) |

### 输入栏主体
| 属性 | I1 默认态 | I2 生成中态 | I3 输入态 |
|------|-----------|-------------|-----------|
| 宽度 | 396px | 396px | 396px |
| 高度 | 52px | 56px | 56px |
| 左偏移 | 16px | 16px | 16px |
| 上偏移 | 10px | 8px | 8px |
| 背景色 | #FFFFFF | #FFFFFF | #FFFFFF |
| 圆角 | 24px | 100px（胶囊形） | 24px |
| 描边 | 0.5px solid rgba(0,0,0,0.10) | 0.5px solid rgba(0,0,0,0.10) | 0.5px solid rgba(0,0,0,0.10) |
| 阴影 | 无 | 0px 4px 16px rgba(0,0,0,0.04) | 无 |

### 结构分区
```
┌─────────────────────────────────────────────┐
│ [左侧图标 28px] [中间内容区 300px] [右侧图标 28px] │
└─────────────────────────────────────────────┘
```

---

## 3. 3 种子组件

### I1 默认态
| 区域 | 内容 |
|------|------|
| 输入栏 | 高度 52px，圆角 24px，无阴影 |
| 左侧 | 图标按钮（28×28px，圆形，白色背景），left: 12px, top: 12px |
| 中间 | 占位文字 "发消息"，left: 48px, top: 14px，17px，color: rgba(60, 60, 67, 0.26) |
| 右侧 | 图标按钮（28×28px，圆形，白色背景），right: 12px, top: 12px |

### I2 生成中态
| 区域 | 内容 |
|------|------|
| 输入栏 | 高度 56px，圆角 100px（完全胶囊形），阴影 0px 4px 16px rgba(0,0,0,0.04) |
| 左侧 | 图标按钮（28×28px，圆形，白色背景），left: 12px, top: 14px |
| 中间 | 音波动画图标（24×23px），left: 52px, top: 16.5px |
| 右侧 | 图标按钮（28×28px，圆形，白色背景），right: 12px, top: 14px |

### I3 输入态
| 区域 | 内容 |
|------|------|
| 输入栏 | 高度 56px，圆角 24px，无阴影 |
| 左侧 | 图标按钮（28×28px，圆形，白色背景），left: 12px, top: 14px |
| 中间 | 输入文字 "你好，我来自腾讯。"，left: 52px, top: 16px，17px，color: rgba(0, 0, 0, 0.90) |
| 右侧 | 图标按钮（28×28px，圆形，白色背景），right: 12px, top: 14px |

---

## 4. 图标按钮规格

### 按钮容器
| 属性 | 值 |
|------|------|
| 尺寸 | 28×28px |
| 背景 | #FFFFFF（白色） |
| 圆角 | 100px（圆形） |

### 内部图标
| 属性 | 值 |
|------|------|
| 尺寸 | 24×24px |
| 位置 | 居中（left: 2px, top: 2px） |

---

## 5. 文字排版

| 类型 | 字号 | 字重 | 颜色 |
|------|------|------|------|
| 占位文字 | 17px | 400 | rgba(60, 60, 67, 0.26) |
| 输入文字 | 17px | 400 | rgba(0, 0, 0, 0.90) |
| 字体 | PingFang SC | - | - |

---

## 6. 属性约束

1. **背景色**：页面背景色使用白色 `#FFFFFF`（bg_bottom_light）
2. **定位**：组件固定在页面底部
3. **毛玻璃效果**：外层容器必须带 `backdrop-filter: blur(25px)` 以融合底部内容
4. **圆角规则**：仅 I2 生成中态使用 100px 胶囊圆角，其余状态为 24px
5. **阴影规则**：仅 I2 生成中态有阴影
6. **图标位置**：左侧图标在 I1 默认态时 top: 12px，在 I2/I3 时 top: 14px

---

## 7. 资源文件

| 资源 | 路径 | 说明 |
|------|------|------|
| 图标按钮 | assets/CodeBuddyAssets/3458_2589/6.svg | 左右两侧圆形图标按钮 |
| 音波图标 | assets/CodeBuddyAssets/3458_2589/4.svg | I2 生成中态的音波动画图标 |

---

## 8. 设计 Token

| Token | 值 | 说明 |
|-------|------|------|
| 输入栏背景 | #FFFFFF | fill_light_secondary |
| 描边色 | rgba(0, 0, 0, 0.10) | border_standard |
| 占位文字色 | rgba(60, 60, 67, 0.26) | text_tertiary |
| 正文文字色 | rgba(0, 0, 0, 0.90) | text_primary |
| 阴影（生成中态） | 0 4px 16px rgba(0, 0, 0, 0.04) | 轻阴影 |
